import {Link} from 'react-router-dom';

const Navbar = () => {
    return (
        <div>
            <nav>
            <Link to="/"> MovieSeriesInfo </Link>
            </nav>
        </div>
    )
}

export default Navbar;